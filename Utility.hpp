#ifndef UTILITY_HPP
#define UTILITY_HPP

#include "Lib.hpp"
#include "Constants.hpp"

int delay(int time);
int randomRange(int min, int max);
int binaryRandom();
void eatMeal (int time);
void runRestaurant();

#endif